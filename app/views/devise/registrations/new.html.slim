-# 新規登録画面
div class=page_classes

  .text-center.mb-4.px-4
    h1.text-lg.sm:text-xl.font-bold.text-pink-700.mb-2 = t("devise.registrations.new.title")
    /みんなで素敵な Mog&Map.を作りましょう！
    p.text-pink-600.text-xs.leading-relaxed = t("devise.registrations.new.subtitle", app_name: t('app.name'))

  .w-full.max-w-sm.mx-auto.flex.flex-col.px-4
    / エラーメッセージ
    .mb-3
      = render "devise/shared/error_messages", resource: resource

      / LINE・Googleでログインセクション
      .text-center.mb-2
        /LINE・Googleで簡単登録
        p.text-pink-700.font-medium.text-xs = t("devise.omniauth.line_or_google")

      .line-login-container.mb-4
        = link_to user_line_omniauth_authorize_path, method: :post, data: { turbo: false }, class: "line-login-button-official flex items-center bg-white rounded-lg shadow-md hover:shadow-lg transition-all duration-200 overflow-hidden", id: "line-login-btn", data: { base_image: asset_path('btn_base.png'), hover_image: asset_path('btn_hover.png'), press_image: asset_path('btn_press.png') } do
          / 左側：LINE公式画像
          .flex-shrink-0
            = image_tag "btn_base.png", alt: "LINE", class: "h-12 w-auto block", id: "line-icon-official"
          / 右側：テキスト部分
          .flex-1.text-center.py-3.px-4.border-l.border-gray-200.hover:brightness-110.transition-all.duration-200 style="background-color: #00B900;"
            / LINEで登録
            span.text-white.font-medium.text-sm = t("devise.omniauth.line_register")

      / Googleログインセクション
      .google-login-container
        = link_to user_google_oauth2_omniauth_authorize_path, method: :post, data: { turbo: false }, class: "flex justify-center" do
          = image_tag "google_sign_up.svg", alt: "Sign in with Google", class: "h-12 w-auto"

    / 区切り線
    .flex.items-center.my-4
      .flex-1.border-t.border-pink-200
      / または
      span.px-3.text-pink-500.text-xs = t("devise.registrations.or")
      .flex-1.border-t.border-pink-200

    / 登録フォーム
    .bg-white.rounded-lg.p-4.shadow-md.border.border-pink-200
      = form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: "space-y-3" }) do |f|
        / お名前入力
        .space-y-1
          .flex.items-center
            .text-xs.mr-1 👤
            / お名前
            label.text-pink-700.font-medium.text-xs = t("devise.forms.labels.name")
          = f.text_field :name, autofocus: true, placeholder: t("devise.forms.placeholders.name"), class: "w-full px-3 py-2 border border-pink-200 rounded-lg focus:outline-none focus:ring-1 focus:ring-pink-300 focus:border-transparent bg-pink-50 text-gray-700 placeholder-pink-400 text-sm"

        / メールアドレス入力（コンパクト化）
        .space-y-1
          .flex.items-center
            .text-xs.mr-1 📧
            / メールアドレス
            label.text-pink-700.font-medium.text-xs = t("devise.forms.labels.email")
          = f.email_field :email, placeholder: t("devise.forms.placeholders.email"), class: "w-full px-3 py-2 border border-pink-200 rounded-lg focus:outline-none focus:ring-1 focus:ring-pink-300 focus:border-transparent bg-pink-50 text-gray-700 placeholder-pink-400 text-sm"

        / パスワード入力
        .space-y-1
          .flex.items-center
            .text-xs.mr-1 🔒
            / パスワード
            label.text-pink-700.font-medium.text-xs = t("devise.forms.labels.password")
          = f.password_field :password, placeholder: t("devise.registrations.new.placeholders.password"), class: "w-full px-3 py-2 border border-pink-200 rounded-lg focus:outline-none focus:ring-1 focus:ring-pink-300 focus:border-transparent bg-pink-50 text-gray-700 placeholder-pink-400 text-sm"

        / パスワード確認入力
        .space-y-1
          .flex.items-center
            .text-xs.mr-1 🔐
            / パスワード確認
            label.text-pink-700.font-medium.text-xs = t("devise.forms.labels.password_confirmation")
          = f.password_field :password_confirmation, placeholder: t("devise.forms.placeholders.password_confirmation"), class: "w-full px-3 py-2 border border-pink-200 rounded-lg focus:outline-none focus:ring-1 focus:ring-pink-300 focus:border-transparent bg-pink-50 text-gray-700 placeholder-pink-400 text-sm"

        / 登録ボタン
        .pt-3
          = f.submit t("devise.forms.buttons.register"), class: "w-full bg-gradient-to-r from-pink-400 to-pink-500 hover:from-pink-500 hover:to-pink-600 text-white font-bold py-2.5 px-4 rounded-lg transition-all duration-200 shadow-md hover:shadow-lg active:scale-95 text-sm"

    / ログインリンク
    .bg-pink-50.rounded-lg.p-3.mt-4.text-center.border.border-pink-200
      p.text-pink-600.text-xs.mb-2
        | すでにアカウントをお持ちの方は
      = link_to "ログイン", new_session_path(resource_name), class: "inline-block bg-white text-pink-600 font-medium py-2 px-4 rounded-lg border border-pink-300 hover:bg-pink-100 hover:border-pink-400 transition-all duration-200 shadow-sm hover:shadow-md text-xs"

